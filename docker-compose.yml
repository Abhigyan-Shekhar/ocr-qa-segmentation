version: '3.8'

services:
  ocr-app:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./models:/app/models
      - ./examples:/app/examples
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
    stdin_open: true
    tty: true
